---

- hosts: all
  sudo: yes
  roles:
    - system
    - ntp
    - ruby
    - php
    - composer
    - apache
    - mysql
    - phpmyadmin
    - ssh
    - git

  vars:
    dbname: erniuniu
    host: light.erniuniu.com
    document_root: "/var/www/{{ host }}"
    mysql_db:
      - name: "{{ dbname }}"
    mysql_users:
      - name: "{{ dbname }}"
        pass: "{{ dbname }}"
        priv: "{{ dbname }}.*:ALL"
        host: "%"
    git_user:
        email: yngwie.chang@gmail.com
        name: Hao Chang
